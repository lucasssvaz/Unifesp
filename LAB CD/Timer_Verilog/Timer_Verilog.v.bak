module Timer(input sysclock, output reg new_clock);

parameter nf = 0.5;
new_clock = 0;
integer aux = ($clog2(50000000/(2*nf)));

reg count[aux:0];

always@(posedge sysclock)
begin
	



end


endmodule
